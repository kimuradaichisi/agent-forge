# Claude hooks

`read-guard.sh` is optional. AgentForge intentionally does not auto-register hooks because existing hook policies vary. Adapt the matcher/input extraction to your current Claude Code hook setup before enabling it.
